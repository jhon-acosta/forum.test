spring:
  application:
    name: forum-api

server:
  port: 8081

forum:
  data-dir: ./data
  default-max-reply-depth: 3
  cors-allowed-origins:
    - http://localhost:4200
    - http://localhost:8082
    - https://*.trycloudflare.com
